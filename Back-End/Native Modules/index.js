// Import the sillyname package
const sillyname = require('sillyname');

// Generate a random silly name
const name = sillyname();

// Print the silly name to the console
console.log(`Your generated silly name is: ${name}`);
